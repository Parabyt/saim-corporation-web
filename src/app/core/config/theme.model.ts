export interface ThemeConfig {
  name: string;
  colors: {
    primary: string;
    primaryDark: string;
    accent: string;
    surface: string;
    mutedText: string;
    headingText: string;
    border: string;
  };
  radius: string;
}
